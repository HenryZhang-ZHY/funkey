{
  "name": "@funkey/interpreter",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc && vite build",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vite": "4.5.3",
    "vitest": "0.33.0"
  }
}
